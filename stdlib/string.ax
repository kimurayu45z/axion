pub struct String
    _ptr: i64
    _len: i64
    _cap: i64

impl String
    fn new() -> String
        String #{_ptr: 0, _len: 0, _cap: 0}

    fn from(s: str) -> String
        String #{_ptr: 0, _len: 0, _cap: 0}

    fn len(self) -> i64
        self._len

    fn is_empty(self) -> bool
        self._len == 0

    fn push(mut self, s: str)
        {}

    fn as_str(self) -> str
        "stub"

    fn drop(mut self)
        {}
